# MyRag 系统依赖（PRD §12.1）
# 解释器：Python 3.11+（开发环境用 3.12）
# 安装：.venv/bin/pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ---- 后端 ----
fastapi
uvicorn[standard]
jinja2
sqlite-vec
scipy
httpx
beautifulsoup4
markdown-it-py

# ---- BGE-M3 向量化（PRD §7.1）----
# 本机 HuggingFace 直连不通，权重走 ModelScope（见 PRD §7.1 / §15）
# FlagEmbedding 1.4.2 自带 transformers<6.0.0 上限；1.3.5 无此上限，会拉到 5.x 而崩
FlagEmbedding==1.4.2
transformers>=4.44.2,<5
torch

# ---- 权重首次下载（运行时只读本地缓存）----
modelscope

# ---- 仅备选模型 bge-large-zh-v1.5 退回 FTS5 方案时需要（PRD §7.1），一期不装 ----
# jieba
